import { DiscountFilterPipe } from './discount-filter.pipe';

describe('DrawerFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DiscountFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
