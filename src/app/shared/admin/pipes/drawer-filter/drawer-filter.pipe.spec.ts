import { DrawerFilterPipe } from './drawer-filter.pipe';

describe('DrawerFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DrawerFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
