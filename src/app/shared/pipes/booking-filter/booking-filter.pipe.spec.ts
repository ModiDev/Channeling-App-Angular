import { DoctorFilterPipe } from './booking-filter.pipe';

describe('DoctorFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DoctorFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
