import { BookingsSortPipe } from './bookings-sort.pipe';

describe('BookingsSortPipe', () => {
  it('create an instance', () => {
    const pipe = new BookingsSortPipe();
    expect(pipe).toBeTruthy();
  });
});
